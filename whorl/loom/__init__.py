"""whorl.loom — CodeCity-Bench topological code analysis.

Status: no implementation in this tree yet. The original Loom scanner lives
in whorl_v0.1.0.tar.gz (concept donor — see runbook.txt before touching it).
Tracked so `import whorl.loom` resolves and the namespace is intentional
rather than an accidental empty shell.
"""

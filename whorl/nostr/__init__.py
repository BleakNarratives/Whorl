"""whorl.nostr — distributed agent intercom via Nostr relays.

Status: no implementation in this tree yet. Planned transport for
cross-machine agent messaging. Tracked so `import whorl.nostr` resolves
and the namespace is intentional rather than an accidental empty shell.
"""
